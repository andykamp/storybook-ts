
import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { CodeCollapse } from '@/docs/CodeCollapse'
import { Card, Spacer } from '@geist-ui/core'
import {Header} from '@/docs/Header.tsx'

<Meta title="design/RoundedCorners" />

 
<Header text={'Rounded Corners'} imgSrc={'src/assets/HeaderDesign.png'}/>

We use border-radius = 6px all over the UI.

A magic trick worth noting is to use parent-border-radius/2 for children. 

<CodeCollapse direction={'column'} textAlignment='flex-start' 
  content={
  <Card width='100%'style={{ background: '#999'}}>
    <Spacer h={4}/>
  </Card>
} code={`<Card width='100%'style={{ background: '#999'}}>
    <Spacer h={4}/>
  </Card>`}/>




