import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { useTheme, Grid, Text, Spacer } from '@geist-ui/core'
import { ColorRow } from '@/docs/ColorRow'
import { ColorSection } from '@/docs/ColorSection'
import {Header} from '@/docs/Header.tsx'

<Meta title="design/Colors" />

 
<Header text={'Colors'} imgSrc={'src/assets/HeaderDesign.png'}/>

The color-scheme is a duplicate of [the Geist-UI component library](https://www.geist-ui.dev/). It is a minimal number of colors to create a simple dark/light theme. It is also easy to costumize (see link)

<Spacer h={1} />

<ColorSection />
